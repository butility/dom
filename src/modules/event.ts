export * from '@/core/event';

import {
    delegate,
    getEventData,
    off,
    on,
    once,
    setEventData,
    trigger,
    triggerEvent,
} from '@/core/event';

const EventUtils = {
    delegate,
    getEventData,
    off,
    on,
    once,
    setEventData,
    trigger,
    triggerEvent,
};

export default EventUtils;
